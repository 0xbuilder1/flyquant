{
  "_readme": "Copy to config.json before running. config.json is gitignored because it carries a live account index; this file is committed and must never have one written into it. NO KEY GOES IN EITHER FILE -- the signer reads LIGHTER_API_KEY from the environment and nothing else does.",
  "_lighterNote": "accountIndex is a PLACEHOLDER until the operator sets it. It is a public number: anyone can read the account at api.rh.lighter.xyz/api/v1/account?by=index&value=<index>, which is exactly what the site links to so a viewer can check the fly's positions without trusting the page. apiKeyIndex 0-3 are reserved by Lighter for its own desktop and mobile interfaces, so a bot uses 4 or above. The api key is a LIGHTER-NATIVE key, not an EVM key, and is registered once against the account.",
  "lighter": {
    "host": "https://api.rh.lighter.xyz",
    "chainId": 4663,
    "accountIndex": 0,
    "apiKeyIndex": 4,
    "maxSlippage": 0.005,
    "signingChainId": 466324,
    "_chainIdNote": "TWO DIFFERENT CHAIN IDS AND THEY ARE NOT INTERCHANGEABLE. 4663 is Robinhood Chain, the L1 where the ZkLighter deposit contract lives -- keeper/harvest.js uses that one. 466324 is Lighter's own L2 behind api.rh.lighter.xyz, and EVERYTHING SIGNED FOR THE EXCHANGE uses it. Signing with 4663 verifies against the wrong domain and returns `21120 invalid signature` -- identical to a wrong key, an empty slot or a malformed order, which is what made it cost three live attempts to find."
  },
  "_tradeNote": "armed is the config seatbelt and --broadcast is the command-line one, and BOTH are required before anything is sent. maxNotionalUsd is a third, enforced inside the signing process itself, so a bug in the sizing cannot spend more than the operator armed whatever the keeper believes. Leave armed false until a full dry cycle has produced orders you agree with.",
  "trade": {
    "armed": false,
    "maxNotionalUsd": 1000,
    "exposure": {
      "_maxFractionNote": "THE OPERATOR'S RISK BUDGET -- the only number in the product that is a policy rather than a measurement. At 1.0 a fully convinced, fully aroused fly puts one equity of NOTIONAL into one market, which at 10x leverage is one slot of margin out of ten. It was 0.25, then 0.5 to clear Lighter's $10 order floor on a $76 account; the slot arithmetic now does that job, so this is back to being a pure ceiling. The fly still moves inside it: conviction times appetite (octopamine against serotonin) decides how much of the allowance it actually takes.",
      "maxFraction": 1.0,
      "maxTotalFraction": 10.0,
      "deadbandFraction": 0.02,
      "staleAfterMinutes": 30,
      "_scaleNote": "THE THREE LIMITS THAT ONLY MATTER WITH REAL MONEY, each measured rather than guessed. maxFraction is PER MARKET; maxTotalFraction caps the SUM, because the fly picks a different market most passes and never revisits the last one -- without it a simulated day on $5,000 reached 1.96x equity across eleven markets, which is leverage nobody asked for. deadbandFraction stops the reconciler chasing conviction noise: re-targeting every 44%-100% swing turned over 133x the account in that same day, about $331/day of slippage on $5,000. staleAfterMinutes closes a position the fly has not faced in that long, because the fly manages one market a pass and the book is not one market.",
      "depthFraction": 0.25,
      "_depthNote": "THE BOUND THAT ONLY EXISTS ONCE THE ACCOUNT IS LARGE, and the one this product would otherwise have got wrong at $100k. A 25% position on $100,000 is $25,000: 4% of BTC's book within +/-0.5%, but 147% of PONS's, 2271% of ANSEM's and 5749% of CASHCAT's -- it eats the whole visible book. Size is therefore capped at this fraction of the THINNER side of the live book, because escape dumps the entire position in one order and a size you can enter but cannot leave is a trap. Read live every pass; a cap from stale depth bounds nothing. An EXIT is never capped.",
      "_deadbandNote": "Applies ONLY to orders that cross. A post-only order pays no spread, so there is nothing to protect it from and blocking it would be the keeper overriding the fly for no gain.",
      "takerMaxBps": 6,
      "_takerNote": "CROSS UNLESS CROSSING IS EXPENSIVE. Lighter charges zero maker AND zero taker fee, so the whole cost of crossing is the spread walked through. The keeper estimates it per order from the live book -- spread/2 plus (notional/depth)*band -- and crosses whenever that is at or under this many basis points. Above it the order rests post-only and accepts that it may not fill, which is a real cost: the first live order rested at the touch, was accepted, and never filled. At 6bp the tight names (SPY 0.3bp, BTC 0.5bp, ETH 1.7bp, CRWV ~10bp/2=5bp) cross and get certainty, while PONS (13.6bp) and USAR (20.2bp) rest.",
      "_maxTotalNote": "The cap on the SUM, in notional, as a multiple of equity. slots x leverage / leverage = slots, so ten slots each allowed one equity of notional is 10x equity in aggregate. This is the number that stops the fly holding a slot in every market it has ever faced.",
      "slots": 10,
      "leverage": 10,
      "_slotNote": "TEN SLOTS OF MARGIN AT 10x, AND THE WHOLE POINT IS THAT IT SCALES BY ITSELF. The account is divided into `slots` equal shares of MARGIN; each position gets one share and levers it by `leverage`, capped by whatever that market allows. At 10 slots and 10x one position is a full equity of notional and ten of them commit the account. Nothing here is a dollar figure, so an account that doubles simply takes positions twice the size with nothing to edit -- growth compounds into size on its own and there is no scaling mechanism anywhere, deliberately. MARGIN IS WHAT IS DIVIDED, NOT NOTIONAL: nine of the 57 markets refuse 10x (PONS, CASHCAT, AI and ANSEM cap at 3x; six more at 5x), and budgeting a fixed notional per slot would quietly demand three slots' margin for a PONS position and have the tenth order rejected while the account looked inside its own cap. THE RISK, STATED PLAINLY: with ten slots full this is 10x the account. A 10% adverse move across the book is a liquidation, not a drawdown.",
      "_leverageNote": "What the operator has set ON THE EXCHANGE with keeper/set-leverage.py. This config value must match what the account actually carries: setting 10 here without pushing it to the exchange sizes positions for margin the account has not been granted, and the orders are simply rejected. Cross margin, so one balance backs every slot."
    },
    "execution": {
      "_note": "HOW ORDERS REACH THE BOOK, and the reason losses should be directional rather than executional. Lighter charges ZERO maker and ZERO taker fee, so the entire cost of trading is the spread -- and only whoever crosses it pays. Opening orders rest POST-ONLY at the touch: they fill at the price we named or not at all, which makes slippage zero by construction instead of small by assumption. The cost traded for it is adverse selection (a resting bid fills exactly when the market comes down on it), which IS directional. An ESCAPE crosses anyway -- an exit that might not fill is not an exit -- and pays half the spread once: 0.25bp on BTC, 6.8bp on PONS. Unfilled orders EXPIRE before the next pass rather than being cancelled, so nothing has to track open orders and the reconciler simply reads what actually filled.",
      "openStyle": "taker below takerMaxBps, post-only above",
      "exitStyle": "market"
    },
    "_maxNotionalNote": "A BUG BACKSTOP, enforced inside the signing process itself, and it is the one number here that does NOT scale with equity -- so it has to be raised by hand as the account grows. The largest legitimate single order is a flip: 2 x one slot's notional = 2 x equity. At $76 that is $152, and 1000 clears everything honest up to about a $500 account while still stopping a sizing bug that tries to send thousands. IF ORDERS START BEING REFUSED AS THE ACCOUNT GROWS, THIS IS WHY."
  },
  "_passNote": "ms is fly time per pass and costs roughly 25x that in CPU. everySeconds is the gap between passes, which is also the market window the fly sees, because most of what it looks at has no historical endpoint.",
  "pass": {
    "ms": 350,
    "everySeconds": 180,
    "_everyNote": "THE BIGGEST COST DIAL, measured rather than guessed. Most orders are NEW markets opened from zero, which no deadband touches -- so the rate the fly switches markets dominates turnover. On a simulated $5,000 day: at 45s it turns over 33x the account (~1.7%/day at 5bp of slippage); at 180s it turns over 7x (~0.35%/day). Slower is cheaper and less responsive; 5bp is an assumption about crossing the spread, not a measurement, and should be re-derived from real fills."
  },
  "_ponsNote": "The token that funds the fly. Fees harvested from the PONS escrow become the collateral it trades. IMMUTABLE ONCE LAUNCHED -- see keeper/README.md. factory and escrow are the PONS V2 constants on Robinhood Chain 4663.",
  "chain": {
    "id": 4663,
    "name": "Robinhood Chain",
    "rpc": "https://rpc.mainnet.chain.robinhood.com",
    "explorer": "https://robinhoodchain.blockscout.com"
  },
  "pons": {
    "factory": "0x7eD598BcEf8bd9Edd8C97A195C6d13f40801EC7e",
    "escrow": "0xd3AFEB2a57f70eF218Aa82451c51B2fb0416Ac9e"
  },
  "launch": {
    "name": "FLYQUANT",
    "symbol": "FLY",
    "logo": "",
    "description": "A complete fruit fly connectome with a wallet. 166,700 reconstructed neurons in a flight arena with 57 live perp markets; it turns toward one and its motor neurons decide the trade. Creator fees fund the account it trades with. Every position is public and every fill links to its transaction.",
    "socials": {
      "twitter": "",
      "telegram": "",
      "discord": "",
      "website": "",
      "farcaster": ""
    },
    "creatorTaxBps": 200,
    "buybackEnabled": false,
    "configId": 0,
    "saltSeed": "flybrain-mainnet-001",
    "_symbolNote": "name and symbol are IMMUTABLE ONCE LAUNCHED. The project is FLYQUANT; the ticker is still FLY. Change symbol here BEFORE launching if you want $FLYQUANT instead -- it cannot be edited after."
  },
  "token": {
    "token": "0x0000000000000000000000000000000000000000",
    "curve": "0x0000000000000000000000000000000000000000",
    "symbol": "FLY",
    "decimals": 18
  },
  "feeWallet": {
    "_note": "The keeper IS the token's creatorFeeRecipient. escrow.claim() pays this address. A FRESH wallet, never one reused from another project, key via PRIVATE_KEY in the environment only.",
    "address": "0x0000000000000000000000000000000000000000"
  },
  "quote": {
    "asset": "native",
    "symbol": "ETH",
    "decimals": 18
  },
  "_distributeNote": "verified gates the UNATTENDED loop and only that. gasReserveEth is the operator's own float floor, which is not fee revenue and may never leave as one.",
  "distribute": {
    "verified": false,
    "gasReserveEth": 0.02,
    "launchBlock": 0
  },
  "loop": {
    "intervalMs": 180000
  },
  "output": {
    "stats": "site/stats.json"
  },
  "watch": {
    "enabled": true
  },
  "fly": {
    "_note": "The sink's own namespace. The core hands this back untouched and never interprets it.",
    "lighter": {
      "accountIndex": 0
    },
    "deposit": {
      "_note": "LEG B, VERIFIED ON CHAIN BY THIS REPO rather than copied from a document. The ZkLighter deposit contract is a PROXY -- its 1,367 bytes contain no selector at all, and checking it alone would have said deposit() does not exist. The EIP-1967 implementation slot points at 0x82de5b1161c93afdfe21ba0d5343f01cd7401d90, whose 23,168 bytes DO contain 0x8a857083.",
      "address": "0x94bAB9693Ba2f6358507eFfcbd372b0660AFfF9d",
      "implementation": "0x82de5b1161c93afdfe21ba0d5343f01cd7401d90",
      "verifiedBy": "eth_getCode on the proxy, EIP-1967 slot read to the implementation, selector 0x8a857083 confirmed present in its bytecode, 2026-09-12",
      "accountOwner": "",
      "_accountOwnerNote": "The L1 address that owns the Lighter account. deposit(_to=this). Set it before arming --arm-fund or the fees credit the fee wallet's own Lighter account instead.",
      "assetIndex": 3,
      "routeType": 0,
      "minDepositUsdg": 1.0,
      "slippageBps": 100
    }
  }
}
